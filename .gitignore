# dependencies
node_modules/

# next.js
.next/
out/

# testing
coverage/

# production
build/

# misc
.DS_Store
*.pem
.env
.env*.local

# SST
.sst/

# debug
npm-debug.log*

# Pipeline v2.0 PR-8f — esbuild output for daemon shell-step `node -e` calls.
# Built by scripts/build-daemon-bundles.mjs (run by scripts/rsync-daemon.sh).
daemon/lib/*-bundle.cjs

# Vendored agentic-office prototype sub-project (own package.json, gltf assets,
# 600+ files). Reference material only — do not commit.
docs/concepts/agentic-office/

# Three.js runtime 3D models (gltf/glb, ~150MB). Host on S3 and load at runtime
# instead of bloating git history.
public/models/

# Vendored external repos for evaluation/reference (jcode, ecc, BMAD-METHOD,
# OpenSpec, superpowers, spec-kit, get-shit-done, ponytail) — ~900MB, each with
# its own .git. Reference material only — never commit.
repos/

# Scoped-typecheck temp artifacts (agents write these during isolated tsc checks)
*.tmp.tsbuildinfo
.tsconfig.check.tmp.json

# v0.dev-generated graph-explorer prototype (reference UI, uses `any` throughout —
# not held to product lint). Same category as agentic-office above.
docs/concepts/v0-graph/
